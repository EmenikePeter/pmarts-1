import { supabase } from './supabase';

// Provide a default export for modules that import `supabaseClient`.
// This keeps compatibility with other code that expects a default export.
export default supabase;
